<UserControl x:Class="CodeSpread.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CodeSpread.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="#222222">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Title -->
        <TextBlock Grid.ColumnSpan="2" Text="Settings" FontSize="20" Foreground="White"
                   FontWeight="Bold" Margin="10"/>

        <!-- Theme Picker -->
        <TextBlock Grid.Row="1" Grid.Column="0" Text="Theme:" Foreground="White" VerticalAlignment="Center" Margin="10"/>
        <ComboBox Grid.Row="1" Grid.Column="1" Width="150" Margin="10"
                  SelectedIndex="0">
            <ComboBoxItem Content="Light"/>
            <ComboBoxItem Content="Dark"/>
            <ComboBoxItem Content="System Default"/>
        </ComboBox>

        <!-- Recent File Storage Path -->
        <TextBlock Grid.Row="2" Grid.Column="0" Text="Recent File Path:" Foreground="White" VerticalAlignment="Center" Margin="10"/>
        <StackPanel Grid.Row="2" Grid.Column="1" Orientation="Horizontal" Margin="10">
            <TextBox Width="250" x:Name="txtFilePath" />
            <Button Content="Browse" Margin="5,0,0,0" Padding="5"/>
        </StackPanel>

        <!-- Limit of Recent Files -->
        <TextBlock Grid.Row="3" Grid.Column="0" Text="Recent Files Limit:" Foreground="White" VerticalAlignment="Center" Margin="10"/>
        <StackPanel Grid.Row="3" Grid.Column="1" Orientation="Horizontal" Margin="10">
            <Slider Width="200" Minimum="1" Maximum="50" Value="10" TickFrequency="1" 
                    IsSnapToTickEnabled="True"/>
            <TextBox Width="50" Text="10" VerticalAlignment="Center" TextAlignment="Center"/>
        </StackPanel>
    </Grid>
</UserControl>
